# Generated by scripts/vendor-daemon-skills.mjs — do not edit by hand.
#
# The daemon embeds these with include_str! and parses their YAML frontmatter.
# A CRLF checkout on Windows broke that parse, so every skill's trigger
# description came out empty: the skills still listed, they just stopped
# matching. The parser is CRLF-tolerant now; this keeps the bytes identical
# across platforms so the vendor digest agrees too.
* text eol=lf
